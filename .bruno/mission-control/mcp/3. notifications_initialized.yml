info:
  name: 3. notifications_initialized
  type: http
  seq: 3

http:
  method: post
  url: http://{{mcpHost}}:{{mcpPort}}/mcp
  headers:
    - name: Accept
      value: application/json, text/event-stream
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer {{mcpApiKey}}
    - name: Mcp-Session-Id
      value: "{{mcpSessionId}}"
    - name: Mcp-Protocol-Version
      value: "{{mcpProtocolVersion}}"
  body:
    type: json
    data: |-
      {
        "jsonrpc": "2.0",
        "method": "notifications/initialized"
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
