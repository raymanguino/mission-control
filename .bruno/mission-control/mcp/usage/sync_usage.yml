info:
  name: sync_usage
  type: http
  seq: 4

http:
  method: post
  url: http://{{host}}:{{port}}/api/usage/sync
  headers:
    - name: Authorization
      value: Bearer {{dashboardJwt}}
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
