info:
  name: get_usage
  type: http
  seq: 1

http:
  method: get
  url: http://{{host}}:{{port}}/api/usage?groupBy=model
  headers:
    - name: Authorization
      value: Bearer {{dashboardJwt}}
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
