info:
  name: delete_channel
  type: http
  seq: 3

http:
  method: delete
  url: http://{{host}}:{{port}}/api/channels/{{channelId}}
  headers:
    - name: Authorization
      value: Bearer {{dashboardJwt}}
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
