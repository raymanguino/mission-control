info:
  name: delete_agent
  type: http
  seq: 5

http:
  method: delete
  url: http://{{host}}:{{port}}/api/agents/{{agentId}}
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
