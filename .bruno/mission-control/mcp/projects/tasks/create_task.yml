info:
  name: create_task
  type: http
  seq: 2

http:
  method: post
  url: http://{{host}}:{{port}}/api/projects/{{projectId}}/tasks
  headers:
    - name: Authorization
      value: Bearer {{token}}
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "title": "Implement feature",
        "description": "Task details",
        "status": "backlog"
      }
  auth: inherit

runtime:
  variables:
    - name: projectId
      value: b76ec9c8-bfeb-4166-95ff-3704fc338bd2

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
