info:
  name: get_intent
  type: http
  seq: 2

http:
  method: get
  url: http://{{host}}:{{port}}/api/intents/{{intentId}}
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
