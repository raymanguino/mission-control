info:
  name: delete_intent
  type: http
  seq: 6

http:
  method: delete
  url: http://{{host}}:{{port}}/api/intents/{{intentId}}
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
