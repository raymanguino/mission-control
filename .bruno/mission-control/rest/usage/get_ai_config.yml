info:
  name: get_ai_config
  type: http
  seq: 3

http:
  method: get
  url: http://{{host}}:{{port}}/api/usage/ai/config
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
