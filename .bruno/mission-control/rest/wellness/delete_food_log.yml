info:
  name: delete_food_log
  type: http
  seq: 5

http:
  method: delete
  url: http://{{host}}:{{port}}/api/health/food/{{id}}
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
