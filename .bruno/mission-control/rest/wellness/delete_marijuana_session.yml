info:
  name: delete_marijuana_session
  type: http
  seq: 8

http:
  method: delete
  url: http://{{host}}:{{port}}/api/health/marijuana/{{id}}
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
