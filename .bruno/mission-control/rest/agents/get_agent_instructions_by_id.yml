info:
  name: get_agent_instructions_by_id
  type: http
  seq: 7

http:
  method: get
  url: http://{{host}}:{{port}}/api/agents/{{agentId}}/instructions
  headers:
    - name: Authorization
      value: Bearer {{dashboardJwt}}
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
