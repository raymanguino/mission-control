info:
  name: get_task
  type: http
  seq: 5

http:
  method: get
  url: http://{{host}}:{{port}}/api/tasks/{{taskId}}
  headers:
    - name: Authorization
      value: Bearer {{dashboardJwt}}
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
