info:
  name: delete_project
  type: http
  seq: 4

http:
  method: delete
  url: http://{{host}}:{{port}}/api/projects/{{projectId}}
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
